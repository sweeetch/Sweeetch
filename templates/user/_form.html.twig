{{ form_start(form, {'attr' : { 'class':'form-center'}}) }}
<div class="form-group">
    {{ form_row(form.email, {'label':'Mail', 'attr' : { class: 'form-control' }} ) }}
</div>
<div class="form-group">        
    {{ form_row(form.password.first, {'type': 'password', 'label' : 'Mot de passe', 'attr' : { class: 'form-control', value:'' }} ) }}
</div>
<div class="form-group">
   {{ form_row(form.password.second, {'type': 'password', 'label' : 'Répéter le mot de passe', 'attr' : { class: 'form-control', value:'' }} ) }} 
</div>
<button class="{{ app.request.get('_route') == 'user_new' ? 'btn-success' : 'btn-warning' }} btn"> {{ app.request.get('_route') == 'user_new' ? 'Enregistrer' : 'Modifier' }} </button>
{{ form_end(form) }}
